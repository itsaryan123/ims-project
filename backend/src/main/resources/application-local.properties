
spring.application.name=InventoryMgtSystem

# Local development: use H2 in-memory database to avoid requiring MySQL
spring.datasource.url=jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

spring.h2.console.enabled=true

# JWT secret used by JwtUtils (test/local only). Use env vars for production.
secreteJwtString=phegondev123456789phegondev123456789
